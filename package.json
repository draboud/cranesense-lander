{
  "name": "cranesense-lander",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild src/main.js --bundle --outfile=dist/main.js --watch",
    "build-by-file": "node 0_builder.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.0",
    "serve": "^14.2.5"
  }
}
